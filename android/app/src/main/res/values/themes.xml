<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- The app is a single WebView activity — this just sets the status
         bar color to match the brand before/around the page loading. -->
    <style name="Theme.Know" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="21">@color/brand_teal</item>
    </style>
</resources>
